Add the next menu images here before we switch the website references.

- `backgrounds/`: menu section background images
- `items/`: food/menu item images

After the new images are added and checked, the old menu images can be removed safely.
